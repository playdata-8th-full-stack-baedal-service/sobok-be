spring:
  application:
    name: cook-service

  config:
    import: optional:configserver:http://config-server:8080

server:
  port: 8080

openapi:
  service:
    url: http://localhost:8000/cook-service